import { Context } from "vm";

export const onVideoDecode = function(decoder:unknown, time:unknown){
    //console.log('time', time);

    // decoder.instance.forEach(element => {
    //     console.log(0);
    // });

    // console.log('time', time);
}

export const checkMotion = function(ctx:Context) {  
    //console.log(ctx);
//     var canvas2 = document.getElementById('myCanvas2');

//     var context = canvas.getContext('2d');
//     var context2 = canvas2.getContext('2d');
//     var imageWidth = canvas.width;
//     var imageHeight =  canvas.height;

  
  
// function draw() {     

//     context2.drawImage(video, 0, 0,imageWidth,imageHeight);

//     var imageData = context2.getImageData(0, 0, imageWidth, imageHeight);
    return "ok";

}